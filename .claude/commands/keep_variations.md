The user specifies the path to the puzzle, to which variations or new parameters was be added. Explore the directory to understand what was added.

Then the user specifies which of the new variations or parameters should be kept in the main index.html version.

Transfer these new parameters/features in the main version, and delete the rest of the .html files in the puzzle directory.